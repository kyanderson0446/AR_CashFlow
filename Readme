The purpose of this project is to build a training model on AR cash flow. We will be utilizing 3 months of data through
the Data Warehouse. The project will then update with a new data set every month and determine when we will receive cash
by each payor for each building.